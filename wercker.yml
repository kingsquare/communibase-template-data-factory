box: wercker/nodejs
services:
    - wercker/mongodb
build:
  steps:
    - add-ssh-key:
        keyname: wercker
    - npm-install
    - npm-test
    - script:
        name: echo nodejs information
        code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"